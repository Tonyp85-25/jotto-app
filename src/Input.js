import React from 'react';
import {connect} from 'react-redux';

const Input = ()=>{
    return <div/>
}

const mapsStateToProps = (state) => {
    return {};
}

export default connect(mapsStateToProps)(Input)